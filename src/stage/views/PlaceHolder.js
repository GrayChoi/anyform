import React from 'react';
import styles from './PlaceHolder.module.css';

export default () => (
  <div className={styles.container}>
    Drag your first question here from the left panel.
  </div>
);