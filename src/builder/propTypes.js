import PropTypes from 'prop-types';

export const color = PropTypes.string;

export const label = PropTypes.string;

export const type = PropTypes.string;

export const dndAction = PropTypes.func;

export const isDragging = PropTypes.bool;